--- a/gpu_tag_detection/gpu_detector.py
+++ b/gpu_tag_detection/gpu_detector.py
@@ -74,9 +74,9 @@
             rectify and one bit read rather than a crop decode.
     """
 
-    #: The proposer is happy with luma and the bit read wants it, so unlike the
-    #: hybrid path there is nothing here that benefits from colour.
-    wants_colour = False
+    #: Preserve BGR for the colour-trained proposer; detect() derives luma
+    #: separately for corner refinement and bit decoding.
+    wants_colour = True
 
     #: Two proposals can cover the same physical tag; reads of the same id within
     #: this many pixels collapse to one detection.
@@ -158,9 +158,9 @@
     def detect(self, frame: np.ndarray) -> list:
         """Detect and read tags in a frame. Returns ``list[Detection]``.
 
-        Colour input is accepted and converted, but luma is the intended path and
-        is strictly cheaper: the proposer's ``letterbox`` replicates luma to three
-        channels *after* the downscale to 640, and the bit read wants grayscale.
+        Keep colour for proposal generation when available. Refinement and
+        bit decoding use a separate grayscale view. Native luma input remains
+        supported for recordings captured in grayscale.
         """
         gray = frame if frame.ndim == 2 else cv2.cvtColor(frame, cv2.COLOR_BGR2GRAY)
 
--- a/ship_camera_processing/worker.py
+++ b/ship_camera_processing/worker.py
@@ -236,8 +236,8 @@
             trt_cache=config.PROPOSER_TRT_CACHE,
             refiner_backend=config.CORNER_REFINER_BACKEND,
             refiner_device=config.CORNER_REFINER_DEVICE,
-            refine_iters=(getattr(args, "refine_iters", None)
-                          or config.CORNER_REFINE_ITERS),
+            refine_iters=(args.refine_iters if getattr(args, "refine_iters", None) is not None
+                          else config.CORNER_REFINE_ITERS),
             max_hamming=config.CORNER_MAX_HAMMING,
             min_contrast=config.CORNER_MIN_CONTRAST,
             max_batch=config.CORNER_MAX_BATCH,
